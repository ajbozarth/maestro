apiVersion: maestro/v1alpha1
kind: Workflow
metadata:
  name: test_creative
  labels:
    app: testapp_model_params
spec:
  template:
    metadata:
      labels:
        app: testapp_model_params
    agents:
      - test_creative
    prompt: Describe the color blue in three words
    steps:
      - name: test_creative
        agent: test_creative

